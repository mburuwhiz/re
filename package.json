{
  "name": "pdf-host-server",
  "version": "1.0.0",
  "description": "Simple server to host a PDF file",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "node -e \"require('./index.js')\" & sleep 2 && kill %1 2>/dev/null; exit 0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1",
    "express": "^4.18.2"
  }
}
